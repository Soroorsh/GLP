output/*
torch_home
env
*.csv
# the Navon congruency manifest is a data artefact, not a result — keep it
!data/navon_test_manifest.csv
# SSL pilot runs: checkpoints and probe curves stay local
runs/
__pycache__/
# result tables are the deliverable — keep them, despite the *.csv rule above
!results/*.csv
# probe token dumps are large binaries, keep them local
results/*.tgz
